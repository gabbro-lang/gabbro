; Exports the self-hosted linker's in-process entry from gabbro.exe when gabld is
; statically linked in (`zig build -Dembed-linker`). link.zig finds it with
; GetProcAddress on gabbro.exe's own module; without it, it loads gabld.dll instead.
EXPORTS
    gabbro_link_mem
