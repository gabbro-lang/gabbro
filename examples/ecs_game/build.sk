#import std.build.{ Build, executable, release, run_step, default };

build :: fn(b: Build) {
    app := b.executable("ecs_game", "src/main.k2");
    app.release();
    b.run_step("run", app);   // k2 build run
    b.default(app);
}
