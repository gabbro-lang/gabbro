#import std.build.{ Build, executable, release, run_step, default };

build :: fn(b: Build) {
    app := b.executable("matrix", "main.k2");
    app.release();
    b.run_step("run", app);
    b.default(app);
}
