#import std.build.{ Build, executable, release, run_step, default };

build :: fn(b: Build) {
    app := b.executable("ecs_game", "src/main.sk");
    app.release();
    b.run_step("run", app);   // skarn build run
    b.default(app);
}
