; Exports the self-hosted linker's in-process entry from skarn.exe when skarnld is
; statically linked in (`zig build -Dembed-linker`). link.zig finds it with
; GetProcAddress on skarn.exe's own module; without it, it loads skarnld.dll instead.
EXPORTS
    skarn_link_mem
